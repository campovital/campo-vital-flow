import { ComingSoonPage } from "@/components/common/ComingSoonPage";

export default function ProgramadorProtocolos() {
  return (
    <ComingSoonPage
      title="Programador de Protocolos"
      description="Reglas y programación de protocolos por fechas/semana/temporada (en construcción)."
    />
  );
}
